using System;
using System.Collections.Generic;
using System.Configuration;
using System.Data.SqlClient;
using System.Linq;
using System.Web;

namespace User_Reg.Models
{
    public class City
    {
/*
        SqlConnection con = new SqlConnection(ConfigurationManager.ConnectionStrings["MyConnection"].ToString());
  
        public IEnumerable<UserClass> GetCityList()
        {
            string maincon = ConfigurationManager.ConnectionStrings["MyConnection"].ConnectionString;
            SqlConnection sqlConnect = new SqlConnection(maincon);
            string query = "SELECT CityName from [dbo][tblcity]";
            SqlCommand sqlcomm3 = new SqlCommand(query, sqlConnect);
            sqlConnect.Open();
            sqlcomm3.ExecuteNonQuery();

            sqlConnect.Close();
        }
*/
    }
}